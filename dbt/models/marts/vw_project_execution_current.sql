{{ config(materialized='view', grants={'select': ['obrasgov_frontend', 'obrasgov_chat']}) }}
select execution.project_id::text, execution.id_execucao_fisica::text, execution.instrument::text, execution.physical_execution_percentage::numeric, execution.execution_registration_at::timestamp, execution.execution_start_date::date, execution.execution_end_date::date, execution.instrument_creation_date::date, execution.source_update_date::date, execution.execution_form::text, execution.indicators::jsonb, execution.reasons::jsonb, execution.source_record_count::bigint, execution.source_updated_at::timestamptz, execution.ingested_at::timestamptz, execution.ingestion_id::uuid
from {{ ref('fct_physical_execution') }} execution inner join {{ ref('int_obrasgov_current_ingestion') }} current on execution.ingestion_id=current.ingestion_id
